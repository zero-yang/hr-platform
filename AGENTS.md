# AGENTS.md

本文件用于指导 AI Agent 在本代码仓库中进行开发、修改、重构、测试和文档维护。

请在执行任何代码变更前阅读并遵守本文件。若本文件与用户的明确指令冲突，以用户当前指令为准；若与更深层目录中的 AGENTS.md 冲突，以更靠近目标文件的 AGENTS.md 为准。

---

## 1. 工作原则

### 1.1 生产环境优先

本项目面向生产环境，所有代码变更都应遵循以下原则：

- 正确性优先于速度
- 可维护性优先于炫技
- 稳定性优先于大规模重构
- 清晰可读优先于过度抽象
- 最小必要改动优先于无关优化

除非用户明确要求，不要进行大范围重构、技术栈替换、架构调整或风格统一。

### 1.2 最小变更原则

修改代码时应：

- 只修改与当前任务直接相关的文件
- 保持现有代码风格、目录结构和命名习惯
- 避免引入无关依赖
- 避免顺手修改无关问题
- 避免格式化整个文件，除非项目已有明确要求

### 1.3 不确定时先确认

当出现以下情况时，应先向用户确认：

- 需求存在明显歧义
- 可能影响线上数据、权限、安全或兼容性
- 需要删除、迁移或重命名大量代码
- 需要引入新的第三方依赖
- 需要修改公共 API、数据库结构或配置协议

如果用户要求你直接处理，应在合理假设下继续，并明确说明你的假设。

### 1.4 交互方式

使用中文与我交互

---

## 2. 项目理解要求

在修改代码前，应先理解相关上下文：

- 阅读相关模块的现有实现
- 查找调用方和被调用方
- 理解数据流、状态流和错误处理方式
- 查看已有测试、类型定义和文档
- 优先复用项目中已有工具函数、组件、hooks、服务、类型和常量

不要在未理解上下文的情况下直接新增平行实现。

---

## 3. 代码风格

### 3.1 遵循现有风格

优先遵循当前仓库已有风格，包括：

- 文件组织方式
- 命名规范
- 错误处理方式
- 日志格式
- 类型定义方式
- 注释习惯
- 测试写法
- API 封装方式

不要因为个人偏好引入新的风格。

### 3.2 命名规范

命名应清晰表达业务含义：

- 避免 `data`、`info`、`temp`、`obj` 等模糊命名
- 避免不必要的缩写
- 布尔值优先使用 `is`、`has`、`can`、`should` 等前缀
- 函数名应表达动作
- 类型名、接口名应表达实体或契约

### 3.3 注释规范

注释应解释“为什么”，而不是重复“做了什么”。

适合添加注释的情况：

- 存在非显而易见的业务规则
- 存在兼容历史逻辑的特殊处理
- 存在性能、安全或边界条件考虑
- 存在第三方服务的特殊约束

不要为显而易见的代码添加噪音注释。

---

## 4. 类型与接口设计

### 4.1 类型安全

如果项目使用 TypeScript、Go、Java、Rust 等强类型语言，应：

- 避免使用 `any`、`unknown` 后直接绕过校验
- 避免不安全的类型断言
- 为外部输入、接口响应、配置项定义明确类型
- 对可空值、异常值、边界值进行显式处理

### 4.2 API 兼容性

修改公共接口时必须谨慎：

- 不随意修改已有字段名、类型和含义
- 不随意删除已有参数或返回值
- 新增字段应尽量保持向后兼容
- 破坏性变更必须明确说明影响范围

---

## 5. 错误处理

### 5.1 不吞错误

禁止无声失败：

```ts
try {
  await doSomething();
} catch (e) {}
```
 
应至少进行以下一种处理：

- 返回明确错误
- 抛出带上下文的异常
- 记录必要日志
- 转换为用户可理解的错误信息
### 5.2 错误信息

错误信息应：

- 说明失败原因
- 包含必要上下文
- 避免泄露敏感信息
- 对用户可见错误保持友好
- 对日志错误保留排查线索

## 6. 安全要求

### 6.1 敏感信息

禁止：

- 将密钥、token、密码、证书写入代码
- 在日志中输出敏感信息
- 将 .env、私钥、凭证文件提交到仓库
- 在前端暴露服务端密钥
- 在错误信息中泄露内部系统细节

敏感信息应通过环境变量、密钥管理服务或项目既有配置机制读取。

### 6.2 输入校验

对外部输入必须进行校验，包括：

- 用户输入
- URL 参数
- 请求体
- 文件上传
- 第三方回调
- 数据库读取后参与关键逻辑的数据

不要相信客户端校验。

### 6.3 权限控制

涉及用户、组织、租户、角色、资源访问时，必须确认：

- 当前用户是否有权限
- 是否存在越权访问风险
- 是否需要租户隔离
- 是否需要服务端二次校验
- 是否可能被构造 ID 越权访问

## 7. 数据库与数据变更

### 7.1 数据库修改

涉及数据库 schema、migration、索引、数据迁移时：

- 不要直接修改生产数据
- migration 应可重复、可追踪、可回滚或至少可补偿
- 避免长时间锁表
- 注意默认值、空值和历史数据兼容
- 大表操作应考虑分批处理
- 新增索引应评估性能影响

### 7.2 查询性能

数据库查询应避免：

- N+1 查询
- 无限制分页
- 不必要的全表扫描
- 在高频路径中执行昂贵聚合
- 返回远超页面需要的数据
- 在循环中重复查询相同数据
- 